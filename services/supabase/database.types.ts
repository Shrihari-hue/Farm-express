/**
 * Placeholder for the Supabase-generated database types.
 *
 * Once the schema in `database/schema.sql` is applied to a real Supabase
 * project (Step 4), regenerate this file with:
 *
 *   npx supabase gen types typescript --project-id <project-id> > services/supabase/database.types.ts
 *
 * Until then we export a permissive `Database` type so the rest of the app
 * compiles against the real shape defined in `types/models.ts`.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
